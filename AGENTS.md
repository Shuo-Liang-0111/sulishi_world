# ZurichWorld 工作约定

- 完整可编辑工程与大型资产仍在 `F:/MyWorld/ZurichWorld`。`H:/MyWorld/ZurichWorld` 是代码仓库副本，缺少场景资产；用户已取消整库资产迁移。不得将现有 Blender、MCP 或贴图根目录自动切到 H 盘。
- 当前状态读取 `CURRENT_STATUS.md`；接续现有 Goal，不重新创建、不缩小验收范围。场景真实性及实际使用优先，必要资料足够后重点改进构造、材质和表面。
- GitHub 为 `https://github.com/Shuo-Liang-0111/sulishi_world`。用户已授权持续同步本项目构建代码、配置和必要记录；每个稳定施工批次检查差异、凭据及暂存文件后提交推送，并将 H 盘副本快进到同一提交。推送失败须明确报告。
- 大型模型、纹理、地图、渲染、环境、缓存及本机凭据不进入 Git；遵循 `.gitignore`。不要把代码同步称为完整场景备份。
- F 盘空间不足尚未由代码复制解决；较大写入前检查容量，保留已保存的有效原生检查点。香港项目只读，不调用外部写码 API。
- F盘完全写满时，允许先将已审核的代码和必要状态记录保存到原本干净的H代码仓库并从H提交推送；不移动场景资产。F恢复后再对齐两个Git工作区，在此之前明确报告不同步，不伪造核验收据。
