# ZurichWorld 工作约定

- 2026-09-25 用户指定 `H:/MyWorld/ZurichWorld` 为唯一活动代码工作目录。后续代码、计划、原生新版本、导出、证据、临时文件和缓存均写 H。显式使用此工作目录；不要因任务最初 cwd 仍为 F:/AAAAAW 而在那里施工。
- `F:/MyWorld/ZurichWorld` 保留历史资产和旧代码快照，作为只读依赖源；没有进行整库资产迁移。Blender、Python、Three.js 现有安装也暂从 F 读取。F 盘必须保持连接，不能删除其模型、贴图、库或运行环境。
- 路径配置见 `workspace.local.json`（不入 Git）；新脚本使用 `tools/workspace_paths.py`。输入逐文件调用 `read_path`，输出使用 `write_path`，不得把输入目录映射当作可写目录。当前启动、027r2/027r3核验与渲染链路已适配；其他历史制作脚本保留原版，包含固定 F 写路径的脚本必须先逐项移植和审查再运行。不得盲目全局替换、批量执行历史脚本或建立回写 F 的目录联接。
- 当前状态读取 `CURRENT_STATUS.md`；接续现有 Goal，不重新创建、不缩小验收范围。场景真实性及实际使用优先，必要资料足够后重点改进构造、材质和表面。
- GitHub 为 `https://github.com/Shuo-Liang-0111/sulishi_world`。用户已授权持续同步本项目构建代码、配置和必要记录；每个稳定施工批次从 H 检查差异、凭据及暂存文件后提交推送。F 不再追求 Git 同步；禁止用旧 F 文件覆盖 H 新稿。推送失败须明确报告。
- 大型模型、纹理、地图、渲染、环境、缓存及本机凭据不进入 Git；遵循 `.gitignore`。不要把代码同步称为完整场景备份。
- 代码迁移没有释放 F 上的大型资产占用。较大写入前检查 H 容量与连接；保留已保存的有效原生检查点。香港项目只读，不调用外部写码 API。
- `tools/python.ps1` 设置 H 工作目录和缓存；`tools/start_blender.ps1` 默认打开配置中的最新已核验施工稿。不要依赖旧007r5实时指针选择建模基底。只运行一个 Blender，长检查若 MCP 回包超时，先查 H 中记录和进程，不能重复提交。
- 后续保存新原生时使用 H/native 下的新版本名，并核验跨盘链接库与纹理；不要对当前从 F 打开的旧稿使用覆盖保存。迁移验证与场景交付验收分开；G1 未完成，保持原区域和自然使用验收要求。
