# 027r2 桥头铺面修订及存储中断

027r2已保存并独立重开；两张实图已核验并查看，第三张因F盘满写成零字节，渲染退出1。本批不是完整视觉验收，更不是G1交付。原027r1工程保留；没有匹配的实时导出或自然使用验收。

## 定位与修订

对027r1原`BD_QA_JUNCTION`机位作16组、每组最多四层像素射线。白线附近首先命中新建铺面，部分白线位于铺面之下。这些暗片不能简单归为摄影残片，因此此次摄影工作块和原始2039块均未修改。

旧高程修订只移动原顶点，长三角面跨越弯曲过渡，铺面和标线插值不一致；岸边外侧只选一个最近三角面，还会在最近面切换处产生厘米级高程跳变。此次沿用已有213面支持的拟合，在岸边外侧改为连续的局部距离加权延拓，保留岸边本身。源资料约束的高度插值仍属于推断，不是新增测量。

322个相关铺面、钢轨、轨槽、路缘和标线网格细分为406124顶点。通常最长边20厘米；两块极窄的地籍碎面在加密时退化，保留经相同检查验证的40厘米划分。最终只有一块窄面作附加高程正则化，21面最大调整0.559毫米；失败候选没有保存为工程。此类处理是几何插值正则化，不能全部称为浮点舍入修复。

原对象、地籍外轮廓、材质、UV尺度、灯光和机位保持。清理重新三角化后的重复面，以及262个双精度交叉积也严格为零的共线面；保留全部顶点和非零面及其各角UV，不丢失可见面积。14个标线对象向实际铺面投射，采用推断的1.5毫米漆膜。少量精确边界射线在0.2毫米邻域复查支撑，XY不移动，复查单独记录。

## 证据与实图

- 准备阶段和应用后的检查一致：188个邻近铺面、8849处同类边界探针，差值中位数0.808毫米、95分位1.244毫米、最大12.058毫米。
- 3953个标线面心均在铺面以上，间距0.641—2.244毫米。原40处对接最大差由6.577毫米降为3.351毫米。
- 474桥面和358下穿探针未发现新增阻挡；12根源杆位置和上下端保持。抬高步道投影1806.719563平方米，道路与轨道2372.592321平方米，原范围保持。
- 新细分铺面坡度99分位8.55%，极窄面仍有24.61%；旧岸边另有更陡小面。本轮不声称无障碍通过、完整摄影碰撞通过或实际使用通过。
- 保存文件`native/G1_027r2_bridge_surface_refinement_working.blend`：95722070字节，SHA256 `7224b963c16a9daad6269c3c713a907f1532e81e88d102f44842f73cdfccf951`，16453对象。磁盘满后重新完整读取该文件，哈希仍一致。
- 独立重开通过三个固定库、24个共享网格、322个新网格指纹和场所检查。`BD_QA_JUNCTION`实图中白线恢复连续，局部折痕减轻，但左侧细暗缝、起伏和单调表皮仍存在。`BD_QA_EAST`桥面保留，周边建筑/旗帜严重摄影失真仍明显。两张PNG完整解码校验通过并实际查看。
- `QB_QA_SOUTH.png`为零字节，不能视为完成。旧批次清单误把空文件哈希列为completed；以实际文件和退出码1为准。新增PNG签名、块CRC、完整像素解码和尺寸检查，已用两张有效图及零字节失败图验证；后续必须重新渲染该对照。

## 重放

从027r1原生开始，依次运行Blender的`blender_prepare_bridge_grade_refinement.py`、项目Python的`prepare_bridge_grade_refinement.py`、Blender的`blender_project_refined_road_markings.py`、`blender_apply_bridge_grade_refinement.py`及`blender_save_bridge_grade_refinement.py`。应用器先检查全部前置指纹，准备全组网格，再应用、清理精确零面积面、记录新指纹并复查。候选的诊断模式不构成验收。

独立复看使用`render_native_views.py`；作者与渲染严格串行。此次已保存源输入、候选和检查记录于F盘`derived/bellevue/bridge_grade`及`evidence/G1_027r2`。H和GitHub只含代码与必要文档。

## F盘中断与恢复顺序

本轮尝试对三份明确历史工程作NTFS透明压缩，但只处理到`G1_012_corner_fixtures_working.blend`时空间耗尽，完整后置读取报磁盘空间不足。其余两份没有进入压缩步骤。不能声称这次压缩释放了空间，也不能声称该历史文件已通过前后哈希核验。卷只读检查报告not dirty，但这不替代该文件完整核验；未作文件系统修复。

已将本次已终止渲染的详细日志作逐字节验证的gzip归档，原文SHA保存在`render_log_archive.json`；gzip保留在同目录，另有系统临时目录的小型副本。没有删除、移动历史原生或恢复工程。后续对旧文件的读取仍受满盘阻断，不继续压缩或反复重读。

已请求用户释放F盘至少10GB，或批准将另外三份非当前依赖的历史工程归档到H后再释放F对应副本。答复前H仍严格代码用途。本轮代码先从H提交同步，F盘Git与文档可能尚未对齐，不能发出F/H同版核验收据。

恢复空间后：先完整核验历史012文件与当前027r2；同步F代码和状态；纠正失败批次清单并重渲染南侧对照；再改进过渡区真实形态/材料，合流同版本实时入口，继续完整G1剩余范围。
