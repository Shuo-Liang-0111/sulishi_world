# H原生稿与实时入口的后续合流

## 2026-09-27：027r15组件和单树浏览器对照

当前原生为`native/G1_027r15_bank_eave_scan_repair.blend`，SHA256 `496397518d867f64a0ea2f8052b0247881ef0bbd9c5aa570a9857e71e6cf2b9d`。组件PID31924于18:30:34 UTC实际退出0、stderr为空，原生没有重存。清单初次93台的结果保留，枚举所有场景相机后PID16688独立运行成功，正确清点99台。逐项检查所属集合后订正遗漏身份：是BF两台及BD四台，SF1相机已在原90集合中。早先缺失相机的证据文件名虽包含SF1，不能据此误认实际对象。

进一步源代码核对发现：239材质、13,510作者对象、39,057,216多边形仅来自Bellevue根，漏掉独立合入的SF1_AUTHOR_ENTRANCE。旧清单保留为runtime_inventory_before_sf1_root_fix.json；新版清点代码显式加入已接收SF1根并验证所有SF1可见非相机构件被覆盖。PID3328从保存r15实际运行，于19:01:37 UTC退出0、stderr为空、原生SHA不变；正确为13,790对象、39,242,842多边形、270材质、99相机，增加280入口对象和31材质。实际节点、socket默认值、连接、纹理、色带及对象使用关系记录runtime_material_graphs.json，属于材质翻译依据，不是翻译完成。问题及解决收据runtime_inventory_scope_issue.json。

累计摄影变化共120块，其中108非空、12空块。新GLB为5,320,580字节、51,137三角形，SHA256 `8c210be97c81c0faa26f0841fb80c19ef46ab3d34bfac0174ad6ffb7b4e993dd`。实际GLB坐标、UV、绕序回读和Three.js的120项切换/恢复及7个守卫通过。此检查不解码摄影纹理，也未把它单独发布到旧作者几何上。当前原生世界输出1024×512线性天空HDR，SHA256 `c52a6b34477a6042c2b60d35838322d0721f8ff21c97cb521e53fb1dcfc07ca8`；未借用外城全景。

树叶单项取BE_TREE_69773_LEAVES，共22,400片、492,800顶点、470,400三角形。先保留逐叶法线而仿射拟合位置，位置最大误差约0.035mm，但实际浏览器四角度RMS最高0.561色值，少量轮廓像素最大差96，拒绝这版；原结果`leaf_affine_browser_failed.json`保留，没有放宽阈值。

随后改为纹理保存精确原始float32位置及逐叶法线，共享每叶拓扑。Node用实际Three.js回读所有顶点和1,411,200三角角点，位置误差0、最大法线角差0.0000162°；实际浏览器四视角三幅逐像素一致，仰视最大差1色值、RMS0.00104。前景像素非空守卫通过，防止两边空画面被判一致。主线实际切换查看总览及同相机近景，控制台无警告/错误；原生稿不改、全场景入口不提升。

精确方案该树属性存储由23,385,600降至12,115,590字节，减少约48.19%。这只是属性字节计数，不是GPU计时、整场景内存或帧率结果。两边试验同用MeshStandardMaterial，原生0.045 SSS尚未实现，不能直接发布为完整树木运行时。其他树种和全体树木也未验证。证据与实际画面见`evidence/G1_027r15/leaf_exact_numeric_roundtrip.json`、`leaf_exact_browser_report.json`、`leaf_exact_visual_review.json`、`leaf_exact_browser_near.png`。

阴影后续补验：精确位置同时接入MeshDepthMaterial及MeshDistanceMaterial，避免默认深度通道只投出重复模板原点。实际浏览器以1024级PCF、零normalBias比较太阳正/反向、点光、太阳自身阴影及点光自身阴影五组。五组均逐像素一致，非零树影分别覆盖5773、12824、12625、20070、27277像素；通过有影/禁投影的差分避免两侧无影假通过。主线实际查看两种光源下原始与紧凑表示，控制台无警告/错误。报告及点光原PNG为leaf_exact_shadow_browser_report.json、leaf_exact_shadow_visual_review.json、leaf_exact_shadow_point.png。仅这棵树在测试条件下的表示通过，未宣称与Cycles灯光/原生SSS等价。

下一步仍须解决阴影/原生材质和全体树木，汇合所有作者集合、灯、相机、碰撞、门与设施，并以当前同版实际游览验证。网页默认007r5/候选018r3仍为旧版本；G1未完成。以下保留r7阶段记录作为施工历史。

2026-09-26。活动工程已经迁H，但当前网页仍是旧007r5默认/018r3候选；不能把代码迁移或最新Blender截图称为实时世界已更新。此记录来自对现有导出代码的实际阅读，不是一项已完成的导出。

下一次同步最新原生至实时入口，须先处理这些实际差异：

1. `export_native_checkpoint.py`和`blender_export_bellevue.py`仍有固定F写根和旧版本限制。按现有`workspace_paths`逐项区分输入与输出，所有新日志、导出与证据写H；不能只替换版本白名单后执行。
2. 旧导出依赖单份`photo_cut_file`。后续桥面、岸边、旗杆与Sternen已经有多次累积摄影裁切，应从当前工作对象与保留源网格推导完整差异并核对材质/UV，避免把旧摄影片重新叠回来。判定空网格要用实际多边形/导出图元数；UV裁切器保留未使用的旧顶点，顶点数非零不证明仍有表面。
3. 最新程序化材料、透明玻璃、灯具和水面不能静默退化。分别记录可由标准PBR/glTF保持的内容，以及需要烘焙或运行时实现的节点；使用相同机位检查。旧代码只有018r3水面动画特例，且只提取少数名称前缀的局部灯，会漏掉新的Sternen室内灯。
4. 相机不能只导出BE/HB前缀或只检查`90_REVIEW_CAMERAS`；BF和BD机位还位于其它集合。枚举全场景相机并记录所属集合，保留原机位、实际眼高、色彩设置及原生文件身份，避免默认首页指向旧版。作者集合也不能只遍历Bellevue根；独立接收的SF1必须纳入。
5. 在副本中评估/导出几何，保持原生可继续编辑。链接库与纹理应先核验；实时碰撞、门和设施状态另需实际使用核验，不能用原生几何检查或视频截图替代。

此清单最初是未实施审计；下面分别更新已完成组件和未完成部分。G1保持原连续区域和自然使用验收要求，不增加研究任务、评分或Agent接口。

## 027r7的实际清点与摄影差分

blender_runtime_inventory.py在保存的027r7场景中清点12,228可见作者对象、38,864,551多边形；其中37,060,208（95.357%）属于树木集合。另有80复查相机、204个材质图（80个复杂图）、21灯、674交互属性标记和一个水面形变对象。交互属性存在不表示设施已可用，材质数量不表示导出已保持。

对2039个保留源节点与当前摄影网格逐一核对，116块有累计变化、其中12块无表面。其余1923块保持；所有对象的源身份、变换与材质对应不变。导出器不重新拼接历次photo_cut_file，而是直接导出当前完整差异，避免复活早期裁掉的扫描。

blender_export_context_delta.py在独立进程38904成功运行并实际退出0，未重存原生。生成G1_027r7_context_delta.glb，5,213,816字节、104网格、50,166三角形，SHA256 1615a55d6a00b70806d23236b68deddcaed7840180ab434cf1fc627646d4ce54。其余12空块显式列入清单。原生绑定SHA256 3e2a7284e719a977c5d32e9d68591027e7ce4f67a426011768997d8fe9cb81cf；输出不带新的摄影材质，保留原图对应关系。

check_context_delta_export.py独立解析实际GLB，按世界坐标/UV/三角形绕序与保存前期望数组逐面匹配，104网格全部通过。不是只检查导出日志。check_photo_delta_binding.mjs使用已安装Three.js实际加载源几何与新差分，116项切换/恢复及原材质对象保留通过；身份、版本、变换、UV等7项故障守卫也通过。此Node检查不解码照片、没有浏览器视觉验收，明确保留这些未验收标志。

网页的摄影替换改为先完整检查再整体启用，发现竞争版本修改时在改变第一项前中止。实际浏览器从H读取新代码打开旧007r5，摄影/实体两次切换及恢复成功，错误/警告日志为空。默认007r5、候选018r3没有提升；新摄影组件单独发布会使作者几何不配套，因此尚未接入默认入口。

首次导出进程3228退出1：glTF的export_format是回调枚举，静态RNA列表为空，参数保护在写出前拦截。检查安装版源码后改为调用其get_format_items，实际读取GLB/GLTF_SEPARATE；材质枚举实际包含NONE。保留失败日志和真实退出码，没有删除证据或覆盖原生。

## 树木数据组织的诊断结果

blender_profile_leaf_instances.py只读分析两棵悬铃木、两棵国槐的叶片。逐叶模板能保持拓扑、UV与颜色比例，仿射位置最大误差0.0177–0.0382毫米；这并不自动保持平滑法线。两棵悬铃木法线最大偏差12.59°/13.63°，国槐约0.40°。没有生成或启用实例化资产，也没有性能改善或视觉等价结论。

下一步应解决逐叶法线/局部形变表示后再逐角度比较；不整体减面近景墙体、窗框、栏杆或设施。还须把所有作者集合、材料、灯、相机、水面和设施状态汇合为同一版本，并实际行走检查。当前摄影组件通过不代表整座世界已经实时同步。

## 重放命令

仅在确认没有其他Blender、且目标导出不存在时运行导出。现有r7资产保留，不重复覆盖；若只想复核，直接运行后两条。

```powershell
./tools/start_blender.ps1 -Background -NativePath H:/MyWorld/ZurichWorld/native/G1_027r7_sternen_street_approaches.blend -Script tools/blender_export_context_delta.py -ScriptArgs @('--profile-leaves') -WaitForExit
./tools/python.ps1 tools/check_context_delta_export.py G1_027r7
node tools/check_photo_delta_binding.mjs G1_027r7
```

证据在evidence/G1_027r7/runtime_inventory.json、context_delta_export.json、context_delta_roundtrip.json、context_delta_binding.json和leaf_instancing_profile.json。几何文件、期望数组、日志及图片在H本地保留，不进代码仓库。
