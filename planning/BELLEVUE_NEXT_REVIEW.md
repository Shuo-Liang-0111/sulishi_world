# Bellevue 下一轮施工

先读 `CURRENT_STATUS.md` 和运行指针；下文保留初期问题的来源，不将旧版本描述当作最新状态。当前原生已到018r3喷泉修订，最新实时候选由 `web/assets/station_preview.json` 标识；默认仍007r5。018r3正在完成匹配导出和材质/动画检查，尚无实际浏览器与自然使用验收。G1保持active，版本提升不等于质量通过。

当前顺序：收好喷泉的原生—导出一致性，随后按连续地面扩大下一批近景修订。优先使用已取得的AV地面、46棵站区树记录和VBZ点线；将未替换树团、地面、树池/路缘和邻近设施按一片空间成批处理。018r3反向视图里仍可见的摄影粘连不能按单机位随意删除，须以被实体替换的物体和真实地面界线限定范围。已确定尺度不重复搜集同类资料，主要时间用于构造、纹理真实尺度、粗糙度变化、接地和多向实看。浏览器工具连接失败期间可继续原生施工，但不能升级默认或宣称实时验收。

1. **西侧过街与岛体**：007r5的70条可见黄线已贴合实体路面，但近景摄影树团和邻接首层极粗糙。按AV岛体、真实树位/树种和步行连接重建，不用不匹配的通用树掩盖。近站区树清单46棵；Poly Haven首次检索返回蓝花楹/松树等不匹配树种，未采用。

   已新增西侧85×100m调查窗，`derived/bellevue/west_context`保留40个相交真实AV面、25条步行线、15个测绘树位及SWISSIMAGE叠图（已实际查看）。13棵Platanus x hispanica、另2棵Sophora；影像为落叶状态，可辅助枝干轮廓，但不要冒称与2025绿色摄影mesh同季。AV459是真实西侧狭长站台，35946为建筑前人行道，不能把它们和道路合成一张平面。保留完整来源多边形，调查窗边界不能变成虚构施工边缘。摄影替换还须检查高度范围；仅用道路平面掩膜会切坏上方树冠，不应继续扩大这种切法。
2. **连续地面**：007r4混凝土/沥青分区已实看；补遮挡下的标线、盖板、导盲与路缘坡端。检查 `platform_join.json` 两个相邻负高差样本（最大约10.5mm），不能只靠封侧面就认定可通行。保留VBZ钢轨线，道岔机械细部未完成。
3. **照明材料**：玻璃黑矩形已通过零强度半球灯独立复现并修复，屋面点状已由32次静止采样改善。剩余重点是真实局部反射、玻璃过度磨砂、过新表面和室内设备细部；不再重复排查已修项。原生和实时都要实看。
4. **自然使用**：地面/围护连通后接步行碰撞、门口障碍保护及日常设施。动画不等于可通行；不设研究任务。
5. **整合**：每批保留当前工作节点，核验所有子集合、身份、几何、图像和重开，再提升统一默认。007r5当前回读1421身份与19项包围盒通过；这不代表其他地区完成。

MCP入口 `tools/blender_mcp_client.py`。源2039块完整保留，道路候选63块差分，旧室内候选13块。导出与回读均需包含 `10_BELLEVUE_RECONSTRUCTION.all_objects`。浏览器大图采用兼容解码路径。原生渲染仅临时分级远处参考贴图，保留近景4K和全部几何，结束恢复；错开GPU压力检查。

历史诊断（非当前待办）：只关画布antialias不等于关透射MSAA。修改透射采样和单面材质的试验未解决，已撤销。最小复现和完整场景后来定位到零强度半球灯；当前修复见 `evidence/G1_006r2/transmission_diagnosis.json`。屋面诊断URL可开关细缝/阴影，但禁止将隐藏几何的诊断URL交付为成品；默认保留全部细缝。
