# 两条施工线的协作边界

2026-09-26，用户授权另开对话修正另一部分，由主任务复核整合。真实性、正常人眼近景和可编辑性标准不变；不因并行缩小G1范围或放松验收。

主任务 `01a08947-06f2-7123-b7c7-c955cfa6c809` 负责 Bellevue / Theaterstrasse20、22 及当前街面修正、总场景检查和 GitHub 提交。第二条线负责 Stadelhofen 站前公共入口附近的一片连续区域，先读现有站区数据、设计和代码，确定与主线不重叠的真实边界，写入自己的状态文件。

已创建第二条线：`01a0dd2f-d4ef-7ef0-a269-3bbfc3ce4d30`，标题“苏黎世并行施工：Stadelhofen站前区域”，local。创建后已确认任务处于active，允许先独立准备；这不代表其建模成果已完成或通过验收。

## 文件和资源

- 活动根目录始终是 `H:/MyWorld/ZurichWorld`。桌面保存的项目标签仍可能指向F，不能据此在F写入。F仅为历史资产和软件输入。
- 第二条线只写 `parallel/stadelhofen_01/`，其中代码和说明可跟踪；native/evidence/derived/sources/runtime的大型产物不入Git。不要修改共享tools、web、CURRENT_STATUS、工作原生指针或其他施工线文件，不执行Git提交/推送。
- 第二条线以主线明确指定的已核验r8为只读基底，不覆盖它。独立输出使用 `SF1_` 对象前缀和独立顶层集合，提交可重放、可审查的增量；禁止把整座城市重复append进总工程。
- 空间分区不一定等于摄影网格文件分区。摄影修改必须交付世界坐标下的有界裁切依据和可在当前工作副本重放的操作，主线核验后组合；不能拿第二条线的旧整块CTX网格覆盖主线已修订的同名块。新增材质同样使用SF1前缀，需要改共享材质时先复制局部实例。
- 本机同时只运行一个Blender。调度记录在 `runtime/coordination/blender_lease.json`；实际进程查询优先于文件记录，过期记录不证明进程停止。主线交出使用权前，第二条线可并行处理参考、测绘、构造方案、源代码和轻量几何，不连接或操控主线MCP，也不自行终止任何进程。
- 第二条线写 `parallel/stadelhofen_01/STATUS.md` 和 `handoff.json` 供主线读取；不必向其他对话发送消息。主线在用户授权的协作范围内发送后续施工安排。

## 合入要求

同用LV95/LN02原点2683775、1246700、400；单位米。保留真实位置、建筑身份与空间连接，明确推断。正常眼高近景、接近路径、反方向及至少一张上下文图，核验地面、入口、构件支撑、材质和接缝。记录使用的基底SHA256、全部新增/更改对象、摄影裁切范围、数据来源和未完成项。

第二条线应交付可重开的局部工程、实际看过的渲染和可复用代码；不能仅交方案或截图便宣布施工完成。总场景合入由主线在新的版本上完成，再查跨区域接缝、旧场景回归、依赖和最终同版实时表现。合入前两条线的成果分别保留，不覆盖已核验版本。

## 用户追加的验收责任

主任务作为总工程师，负责独立读取对方源码、重开局部工程、查看多角度原始图像与所依据资料，检查构造/材质、地面/入口/通路连接及合并影响。第一块为试合作，完成报告不是验收依据。若交付质量达不到当前标准，不继续派发下一块，保留并标明未合格成果，由主线接手；若验收通过，再安排下一块明确且隔离的施工范围，每批仍须复核。用户已授权主线在这一协作流程中向第二条线发送后续安排，不需要每次再向用户申请分派许可。
